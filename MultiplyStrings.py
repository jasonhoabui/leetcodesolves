class Solution:
    def multiply(self, num1: str, num2: str) -> str:
        n = int(num1)
        m = int(num2)
        return str(n * m) #return to this when you can do it without using int() LMFAO
